eval(read("examples/functions.mky"))

let trap = macro(body: string, end: ["\n", ";"]) {
	`
	try:
		$body
	except Exception as e:
		print(e)
	`
}

// The emacs problem, why is it so good at being hacky (spoiler: is simple, is all the same thing a buffer)
trap(`file = open('test.txt')`)
