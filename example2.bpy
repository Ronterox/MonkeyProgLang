#!/usr/bin/bepy
// Better Python

fn function(name str, age int) {
    println f"Hi i'm %s i have %d" name age
    for i in 1..age {
        if i % 2 == 0:
            println "I had " + i + " years"
    }

    println "I had $it years" in dataset if it % 2 == 0

    words := name.split(" ")
    for w in words {
        println f"My name %s is spelled with" w
        for c in w {
            print ',' + c
        }
    }
}

function("Ricardo Montserrat", 10)
